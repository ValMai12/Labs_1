/**
 * DiscountType Enum
 *
 * Used in: Discount entity (type field)
 *
 * Values:
 * - percent: Discount is a percentage of the price
 * - fixed: Discount is a fixed amount
 */
const DiscountType = Object.freeze(["percent", "fixed"]);

export default DiscountType;
